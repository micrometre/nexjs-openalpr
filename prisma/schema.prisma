generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:../sqlite-data/collection.db"
}

model items {
  id    Int     @id @default(autoincrement())
  plate String?
  uuid  String?
}
